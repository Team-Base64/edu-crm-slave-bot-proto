syntax = "proto3";

package slavebotgrpc;
option go_package = "github.com/Team-Base64/slavebotgrpc";

message Message {
    string text = 1;
}

service SlaveBot {
    rpc StartChat(stream Message) returns (stream Message);
}
