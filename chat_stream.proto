syntax = "proto3";

package chatstream;
option go_package = "github.com/Team-Base64/edu-crm-chat-service-slave-bot-proto";

message Message {
    string text = 1;
}

service ChatStream {
    rpc StartChat(stream Message) returns (stream Message);
}
